Hi! I’m attaching a screenshot of the current homepage hero. Right now the top section feels very gray/flat, and I want it to feel warmer + more whimsical while keeping the text readable.
1) Add a full-width background image to the hero
In the top hero section (the section with “FilmPsych”, the description, and the 3 buttons), please add a background image that:
covers the entire hero area (full width + full height of that section)
uses background-size: cover and background-position: center
has a lower opacity / darker overlay so the text is readable
I’d like the image to sit beneath the text (as a background), not below the hero as a separate block.
Implementation preference:
Use an overlay layer (recommended) rather than lowering the opacity of the image directly, so the text opacity stays 100%.
Example structure:
Background image layer
Dark/soft overlay layer (ex: black or deep purple at ~35–55% opacity, or a subtle gradient)
Text/content layer on top
2) Add a “glass”/soft highlight behind the main title
Please add a soft pill/rounded rectangle behind the large “FilmPsych” title (and optionally behind the paragraph too), like a light “glass” card so it pops.
Something like:
background: white with ~10–20% opacity
blur/backdrop blur if possible
subtle border (white at ~15–25% opacity)
rounded corners, soft shadow
This should make the title the most readable element.
3) Update the color palette to match the image and feel less gray
Adjust the hero background/overlay + button colors so everything feels cohesive with the new background image.
Goal: less gray, more warm + whimsical (FilmPsych purple can stay as a main brand color).
Please ensure button styles still pass readability/contrast.
4) Add subtle sparkle animation in the background
I want light, slow-moving sparkles floating around in the hero background for a whimsical feel.
Requirements:
Very subtle (not distracting)
Low opacity
Slow drift + gentle fade in/out
Should stay behind the content (no overlapping the text heavily)
Can be implemented with:
CSS keyframes + pseudo-elements, or
a lightweight canvas animation, or
small SVG particles
Please respect accessibility: if possible, disable/reduce motion when prefers-reduced-motion is enabled.
Success criteria
The hero immediately feels less gray, more alive + storybook-like
Text is still extremely readable
The “FilmPsych” title has the strongest emphasis
Sparkles feel subtle and premium